import React from 'react'
import Title from 'Components/title'

const Provider = () => {
    return (
        <Title typing={true}>
            Provider page
        </Title>
    )
}

export default Provider
